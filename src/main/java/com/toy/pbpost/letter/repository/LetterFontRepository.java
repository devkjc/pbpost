package com.toy.pbpost.letter.repository;

import com.toy.pbpost.letter.domain.LetterFont;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LetterFontRepository extends JpaRepository<LetterFont, Long> {
}
