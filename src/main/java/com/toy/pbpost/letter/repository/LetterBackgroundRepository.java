package com.toy.pbpost.letter.repository;

import com.toy.pbpost.letter.domain.LetterBackground;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LetterBackgroundRepository extends JpaRepository<LetterBackground, Long> {
}
